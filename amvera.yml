meta:
  environment: node
  toolchain:
    name: npm
    version: "20"

build:
  additionalCommands: npm run build

run:
  command: HOSTNAME=0.0.0.0 npm run start
  persistenceMount: /data
  containerPort: 3000
